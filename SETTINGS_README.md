# 智能桌面宠物 - 功能说明

## 🎨 现代化 UI 设计

### 界面特点
- **圆角窗口设计**：去除生硬的边框感，使用柔和的浅灰背景（#F5F6F7）
- **现代化 Tab 选项卡**：选中状态有明显的底部蓝色线条（#007AFF），字体加粗
- **美化按钮**：保存按钮实心填充主色调，取消按钮描边样式，悬停效果
- **优化输入框**：固定高度 35px，焦点变蓝，眼睛图标切换密码可见性

## 🤖 多 AI 模型切换功能

### 支持的 AI 模型
1. **本地对话 (免费)** - 无需 API Key，支持基本问答
2. **DeepSeek** - 免费注册，性价比高
3. **OpenAI (ChatGPT)** - 需要绑定支付方式
4. **豆包 (Doubao)** - 火山引擎平台
5. **通义千问 (Qwen)** - 阿里云 DashScope
6. **Kimi** - Moonshot 平台

### 功能特点
- **免费本地对话**：默认使用免费本地对话，无需配置 API Key
- **模型选择器**：页面顶部下拉框，可快速切换 AI 模型
- **自动更新**：切换模型时，API Key 标签和默认 URL 自动更新
- **独立配置**：每个模型的 API Key 和 URL 独立保存

## 💬 AI 对话功能（单击宠物）

### 功能特点
- **免费对话**：默认使用免费本地对话，无需 API Key
- **实时对话**：与 AI 进行实时对话
- **智能回复**：根据关键词匹配智能回复
- **模型切换**：在设置中切换不同的 AI 模型

### 使用方法
1. 单击宠物图标打开对话窗口
2. 输入你的问题
3. 按 Enter 或点击"发送"按钮
4. AI 会自动回复你的问题

## 🎉 随机问候（双击宠物）

### 功能特点
- **时间感知**：根据当前时间显示不同的问候语
- **温馨提醒**：包含天气、健康、心情等提醒
- **随机变化**：每次双击都有不同的问候

### 问候示例
- 早上好！新的一天开始了！☀️
- 中午好！记得吃午饭哦～
- 晚上好！放松一下吧～
- 夜深了，早点休息哦～🌙

## 📊 系统状态监控（右键菜单）

### 功能特点
- **实时监控**：CPU、内存、磁盘使用率
- **状态评估**：根据使用率评估系统状态
- **网络流量**：显示网络发送/接收流量
- **电池信息**：显示电池电量和充电状态（笔记本）

### 状态指标
- 🟢 绿色：系统运行良好（CPU/内存 < 70%）
- 🟡 黄色：系统运行正常（CPU/内存 70-90%）
- 🔴 红色：系统负载较高（CPU/内存 > 90%）

## 📍 城市设置功能

### 定位模式
- **自动定位**：通过 IP 地址自动获取当前城市（支持主备两个 API）
- **手动设置**：手动输入城市名称（支持中英文）

## 🖱️ 交互方式

### 鼠标操作
- **左键单击**：打开 AI 对话窗口
- **左键双击**：显示随机问候
- **左键拖拽**：移动宠物位置
- **右键单击**：弹出菜单（AI 对话、天气查询、系统状态、设置、退出）

### 右键菜单选项
- 💬 AI 对话
- 🌤️ 查询天气
- 📊 系统状态
- ⚙️ 设置
- 🚪 退出

## 📁 文件结构

```
smart_desktop_pet/
├── main.py              # 应用入口
├── pet_window.py        # 桌面宠物主窗口
├── settings_dialog.py   # 设置对话框（现代化重写）
├── chat_dialog.py       # AI 对话窗口
├── ai_service.py        # AI 对话服务（支持免费本地对话）
├── system_status.py     # 系统状态监控（新增）
├── weather_service.py   # 天气查询服务
├── db_manager.py        # 数据库管理
├── config.py            # 配置文件
└── pet_settings.db      # 用户设置数据库
```

## 🚀 使用方法

### 启动应用
```bash
python main.py
```

### AI 对话（免费）
1. 单击宠物图标打开对话窗口
2. 直接输入问题即可
3. 默认使用免费本地对话，无需配置

### 使用 API 对话
1. 双击宠物或右键菜单打开设置
2. 在"API 配置"标签页选择要使用的 AI 模型
3. 输入对应的 API Key
4. 保存设置后即可使用

### 查看系统状态
1. 右键点击宠物
2. 选择"系统状态"
3. 查看 CPU、内存、磁盘等信息

### 查询天气
1. 右键点击宠物
2. 选择"查询天气"
3. 查看当前城市天气信息

## 🔧 自定义配置

### 添加新 AI 模型
在 `ai_service.py` 的 `AI_MODELS` 字典中添加新模型的配置：
```python
AI_MODELS["新模型名称"] = {
    "default_url": "https://api.example.com/v1/chat/completions",
    "model": "model-id",
    "is_free": False,
}
```

### 修改本地对话回复
在 `ai_service.py` 的 `LOCAL_RESPONSES` 字典中修改回复内容：
```python
LOCAL_RESPONSES = {
    "问候": ["自定义问候语1", "自定义问候语2"],
    "天气": ["自定义天气回复"],
    # ...
}
```

## 🐛 常见问题

### Q: 为什么对话没有回复？
A: 默认使用免费本地对话，无需 API Key。如果需要更智能的回复，可以在设置中配置 API Key。

### Q: 如何切换 AI 模型？
A: 打开设置对话框，在"API 配置"标签页中选择要使用的模型。

### Q: 系统状态显示不准确？
A: 系统状态是实时监控的，数据可能会有轻微延迟。

### Q: 自动定位不工作？
A: 自动定位依赖网络连接，如果定位失败会使用备用 API。如果仍然失败，请手动设置城市。

## 📝 更新日志

### v4.0 (2026-09-10)
- 新增免费本地对话功能，无需 API Key
- 双击宠物改为随机问候（时间感知）
- 新增系统状态监控功能（右键菜单）
- 优化 AI 对话，支持免费和 API 两种模式
- 改进用户体验和交互反馈

### v3.0 (2026-09-10)
- 新增 AI 对话功能，单击宠物即可对话
- 重写设置对话框，采用现代化 UI 设计
- 添加多 AI 模型切换功能（5 种模型）
- 优化城市设置界面，修复自动定位问题
