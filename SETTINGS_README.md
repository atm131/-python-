# 智能桌面宠物 - 功能说明

## 🎨 现代化 UI 设计

### 界面特点
- **圆角窗口设计**：去除生硬的边框感，使用柔和的浅灰背景（#F5F6F7）
- **现代化 Tab 选项卡**：选中状态有明显的底部蓝色线条（#007AFF），字体加粗
- **美化按钮**：保存按钮实心填充主色调，取消按钮描边样式，悬停效果
- **优化输入框**：固定高度 35px，焦点变蓝，眼睛图标切换密码可见性

## 🤖 多 AI 模型切换功能

### 支持的 AI 模型
1. **本地对话 (免费)** - 无需 API Key，支持基本问答
2. **DeepSeek** - 免费注册，性价比高
3. **OpenAI (ChatGPT)** - 需要绑定支付方式
4. **豆包 (Doubao)** - 火山引擎平台
5. **通义千问 (Qwen)** - 阿里云 DashScope
6. **Kimi** - Moonshot 平台

### 功能特点
- **免费本地对话**：默认使用免费本地对话，无需配置 API Key
- **模型选择器**：页面顶部下拉框，可快速切换 AI 模型
- **自动更新**：切换模型时，API Key 标签和默认 URL 自动更新
- **独立配置**：每个模型的 API Key 和 URL 独立保存

## 💬 AI 对话功能（单击宠物）

### 功能特点
- **免费对话**：默认使用免费本地对话，无需 API Key
- **实时对话**：与 AI 进行实时对话
- **智能回复**：根据关键词匹配智能回复
- **模型切换**：在设置中切换不同的 AI 模型

### 使用方法
1. 单击宠物图标打开对话窗口
2. 输入你的问题
3. 按 Enter 或点击"发送"按钮
4. AI 会自动回复你的问题

## 🌤️ 天气查询（双击宠物 / 右键菜单）

### 功能特点
- **双击即查**：双击宠物，天气气泡显示在宠物头顶
- **出行建议**：根据温度、湿度、天气现象自动生成建议（带伞 / 防晒 / 添衣 / 戴口罩等）
- **中文描述**：wttr.in 返回的英文天气描述会翻译为中文（23 种常见天气）
- **多级容灾**：4 个 IP 定位接口依次尝试，任一成功即返回
- **自动淡出**：气泡默认 15 秒后渐隐消失

### 气泡内容示例
```
📍 成都
🌡️ 20.0℃（体感 20.0℃）
🌤️ 附近有零星小雨
💧 湿度 63%
💡 今天有雨，记得带伞哦！☂️
```

## 🐳 宠物形象与状态切换

桌宠「小d」内置 7 套表情立绘（`assets/states/`），会根据你正在用的功能自动换表情：

| 状态 | 表情 | 什么时候出现 |
|---|---|---|
| 待机 | 眯眼微笑 | 默认；其它表情过几秒后自动回到这个 |
| 打招呼 | 举手挥挥 | 程序启动时 |
| 对话中 | 眨眼俏皮 | 打开 AI 对话窗口 |
| 查天气 | 惊讶摊手 | 查询天气 |
| 系统状态 | 半月眼 | 查看系统状态 |
| 高负载 | 晕乎乎吐舌 | CPU / 内存持续过高 |
| 开心 | 闭眼唱歌 | AI 回复到达时 |

想换素材：把新图片放进 `assets/states/`，文件名保持不变即可（要求透明背景 PNG）。
各表情的相对大小可在 `pet_window.py` 的 `PET_STATES` 表第 3 列调整。

## 📊 系统状态监控（右键菜单）

### 功能特点
- **实时监控**：CPU、内存、磁盘使用率
- **状态评估**：根据使用率评估系统状态
- **网络流量**：显示网络发送/接收流量
- **电池信息**：显示电池电量和充电状态（笔记本）

### 状态指标
- 🟢 绿色：系统运行良好（CPU/内存 < 70%）
- 🟡 黄色：系统运行正常（CPU/内存 70-90%）
- 🔴 红色：系统负载较高（CPU/内存 > 90%）

## 📍 城市设置功能

### 定位模式
- **自动定位**：通过 IP 地址自动获取当前城市（ipinfo.io → ip-api.com → ip.seeip.org → ipapi.co 四级容灾）
- **手动设置**：手动输入城市名称（支持中英文，内置 45 个城市中英文映射）

## 🖱️ 交互方式

### 鼠标操作
- **左键单击**：打开 AI 对话窗口
- **左键双击**：查询天气并显示出行建议
- **左键拖拽**：移动宠物位置（松手即自动保存位置）
- **右键单击**：弹出菜单（AI 对话、天气查询、系统状态、设置、退出）

### 右键菜单选项
- 💬 AI 对话
- 🌤️ 查询天气
- 📊 系统状态
- ⚙️ 设置
- 🚪 退出

## 📁 文件结构

```
smart_desktop_pet/
├── main.py              # 应用入口（组装模块、启动监控线程与插件系统）
├── pet_window.py        # 桌面宠物主窗口（气泡 / 点击 / 拖拽 / 右键菜单）
├── chat_dialog.py       # AI 对话窗口（支持 /天气 等插件命令）
├── settings_dialog.py   # 设置对话框（城市设置 + 多模型 API 配置）
├── ai_service.py        # AI 对话服务（免费本地对话 + 5 个 API 模型）
├── system_status.py     # 系统状态监控（CPU/内存/磁盘/网络/电池）
├── system_monitor.py    # 后台监控线程（定时采集 + 阈值事件，见 README.md）
├── weather_service.py   # 天气查询服务（wttr.in + 4 个 IP 定位接口）
├── plugin_manager.py    # 事件总线与插件管理框架
├── plugins/             # 插件目录（weather_plugin.py 提供 /天气 命令）
├── rag_engine.py        # 本地知识库与流式 LLM 客户端（可选扩展）
├── pet_ui.py            # 动画状态机版宠物 UI（独立演示，python pet_ui.py）
├── db_manager.py        # SQLite 设置持久化
├── config.py            # 全局配置（UI / 宠物 / 监控 / RAG / LLM）
├── assets/pet.png       # 备用形象（已抠去白底，透明 PNG）
├── assets/pet.ico       # 应用图标（16~256 共 7 种尺寸）
├── assets/states/       # 7 套表情立绘（idle/greet/chat/weather/status/alert/happy）
├── tools/               # 离线构建脚本（cutout_states.py 重新生成表情立绘）
└── pet_settings.db      # 用户设置数据库（运行时生成）
```

模块职责与依赖关系详见 [README.md](README.md)。

## 🚀 使用方法

### 启动应用
```bash
python main.py
```

### AI 对话（免费）
1. 单击宠物图标打开对话窗口
2. 直接输入问题即可
3. 默认使用免费本地对话，无需配置

### 使用 API 对话
1. 右键点击宠物 → 选择"设置"
2. 在"API 配置"标签页选择要使用的 AI 模型
3. 输入对应的 API Key
4. 保存设置后即可使用

### 查看系统状态
1. 右键点击宠物
2. 选择"系统状态"
3. 查看 CPU、内存、磁盘等信息

### 查询天气
1. 右键点击宠物
2. 选择"查询天气"
3. 查看当前城市天气信息

## 🔧 自定义配置

### 添加新 AI 模型
在 `ai_service.py` 的 `AI_MODELS` 字典中添加新模型的配置：
```python
AI_MODELS["新模型名称"] = {
    "default_url": "https://api.example.com/v1/chat/completions",
    "model": "model-id",
    "is_free": False,
}
```

### 修改本地对话回复
在 `ai_service.py` 的 `LOCAL_RESPONSES` 字典中修改回复内容：
```python
LOCAL_RESPONSES = {
    "问候": ["自定义问候语1", "自定义问候语2"],
    "天气": ["自定义天气回复"],
    # ...
}
```

## 🐛 常见问题

### Q: 为什么对话没有回复？
A: 默认使用免费本地对话，无需 API Key。如果需要更智能的回复，可以在设置中配置 API Key。

### Q: 如何切换 AI 模型？
A: 打开设置对话框，在"API 配置"标签页中选择要使用的模型。

### Q: 系统状态显示不准确？
A: 系统状态是实时监控的，数据可能会有轻微延迟。

### Q: 自动定位不工作？
A: 自动定位依赖网络连接，如果定位失败会使用备用 API。如果仍然失败，请手动设置城市。

## 📝 更新日志

### v3.1 (2026-09-11)
- 宠物更名为「小d」
- 新增 7 套表情立绘（全部从原始素材抠图：泛洪去白底 + 种子点清气泡 + 连通域去浮层）
- 新增状态切换机制：启动 / 对话 / 天气 / 系统状态 / 高负载 / 收到回复 各自换表情，
  超时自动回到待机；插件可通过 `pet.state` 事件驱动
- 修复 chat_dialog.py 直接运行时缺少 QApplication 导入的问题

### v3.0 (2026-09-11)
- 修复天气/系统状态气泡不可见的问题（气泡改为独立顶层窗口）
- 新增后台系统监控线程，CPU/内存持续高负载时主动气泡提醒
- 新增插件系统，对话窗口支持 `/天气 北京`、`/weather Shanghai` 命令
- 修复设置窗口不恢复已保存 AI 模型的问题
- 修复对话窗口"正在思考中"占位消息残留的问题
- 拖拽结束后立即保存宠物位置（不再依赖正常退出）
- 宠物图片改为项目内相对路径 assets/pet.png
- 用 nv.png 作为宠物形象与程序图标：抠除白色背景（泛洪填充，保留白围裙），
  并生成多尺寸 assets/pet.ico 用作窗口图标
- 优化 IP 定位接口顺序为实测最优，定位更快
- 系统状态查询不再阻塞界面 1 秒
- 修复数据库连接未关闭导致的句柄泄漏

### v2.0 (2026-09-10)
- 新增免费本地对话功能，无需 API Key
- 新增系统状态监控功能（右键菜单）
- 新增设置对话框：城市设置 + 多 AI 模型切换
- 优化 AI 对话，支持免费和 API 两种模式

### v1.0 (2026-09-10)
- 桌面宠物基础功能：透明窗口、拖拽、右键菜单
- 天气查询与出行建议
- 系统状态展示
